define(function(require) {
  'use strict';

  var angular = require('angular');

  // agular module definition
  return angular.module(
    // module name
    'fend.navbar',

    // module dependencies
    [

    ]
  );

});
